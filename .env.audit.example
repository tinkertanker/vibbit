# Copy this file to `.env.audit` (which is gitignored) and fill in secrets.
# You can also export these variables in your shell/CI instead.

# Optional: require live audits to fail when no secrets are present.
# AUDIT_REQUIRE_SECRETS=1

# Optional: choose provider explicitly: openai | gemini | openrouter
AUDIT_BYOK_PROVIDER=openai

# BYOK provider keys/models (only set what you intend to use)
AUDIT_BYOK_OPENAI_KEY=
AUDIT_BYOK_OPENAI_MODEL=gpt-4o-mini

AUDIT_BYOK_GEMINI_KEY=
AUDIT_BYOK_GEMINI_MODEL=gemini-2.5-flash

AUDIT_BYOK_OPENROUTER_KEY=
AUDIT_BYOK_OPENROUTER_MODEL=openrouter/auto

# Managed runtime endpoint override (optional)
AUDIT_MANAGED_BACKEND=
AUDIT_MANAGED_APP_TOKEN=

# Optional prompts for live checks
AUDIT_MANAGED_PROMPT=Create a tiny MakeCode micro:bit program that flashes an LED dot.
AUDIT_BYOK_PROMPT=Create a tiny MakeCode micro:bit program that shows a heart icon forever.
