{
  "name": "vibbit-extension",
  "version": "0.2.3",
  "private": true,
  "description": "Build tooling for the Vibbit Chrome extension",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node --watch scripts/build.mjs",
    "build:hosted": "VIBBIT_BUILD_PROFILE=hosted-managed VIBBIT_BACKEND=https://vibbit.tk.sg node scripts/build.mjs",
    "build:bookmarklet": "npm run check:compat-core && npm --prefix apps/bookmarklet run build",
    "build:bookmarklet:byok": "npm run check:compat-core && npm --prefix apps/bookmarklet run build -- --enable-byok",
    "sync:compat-core": "node scripts/sync-compat-core.mjs",
    "check:compat-core": "node scripts/sync-compat-core.mjs --check",
    "test": "node --test",
    "dev:watch-reload": "node scripts/dev/watch-build-reload.mjs",
    "package": "npm run check:compat-core && npm run build:hosted && node scripts/package.mjs",
    "package:neutral": "npm run check:compat-core && npm run build && node scripts/package.mjs",
    "backend:start": "node apps/backend/src/server.mjs",
    "backend:dev": "node --watch apps/backend/src/server.mjs",
    "audit:smoke": "node scripts/audit/smoke.mjs",
    "audit:editor": "node scripts/audit/editor-validation.mjs",
    "audit:extension": "node scripts/audit/extension-boundary.mjs",
    "audit:live": "node scripts/audit/live.mjs",
    "audit:install": "npx playwright install chromium"
  },
  "dependencies": {
    "makecode": "^1.3.5"
  },
  "devDependencies": {
    "canvas": "^3.2.1",
    "playwright": "^1.58.2"
  }
}
